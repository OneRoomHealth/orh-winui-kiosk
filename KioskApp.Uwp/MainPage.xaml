<Page
    x:Class="KioskApp.Uwp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KioskApp.Uwp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <!-- Main WebView2 -->
        <muxc:WebView2 x:Name="RootWebView" 
                       Source="{x:Bind KioskUrl}"
                       HorizontalAlignment="Stretch" 
                       VerticalAlignment="Stretch"/>

        <!-- Offline error page (hidden by default) -->
        <Frame x:Name="OfflineFrame" Visibility="Collapsed"/>

        <!-- Transparent tap hotspot in top-right corner for exit gesture -->
        <Grid x:Name="TapHotspot"
              HorizontalAlignment="Right"
              VerticalAlignment="Top"
              Width="120"
              Height="120"
              Background="Transparent"
              Tapped="TapHotspot_Tapped"/>
    </Grid>
</Page>
